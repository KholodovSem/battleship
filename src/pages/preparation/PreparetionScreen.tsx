import { Harbor } from '@/components/Harbor';
import { Board } from '@/components/Board';

export const PreparetionScreen = () => {
  return (
    <div className="flex">
      <Harbor />
      <Board />
    </div>
  );
};
